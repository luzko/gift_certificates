dependencies {
    implementation project(":persistence")
    implementation "org.modelmapper:modelmapper:${mapper}"
    implementation "org.springframework.boot:spring-boot-starter-validation"
}

bootJar {
    enabled = false
}

jar {
    enabled = true
}
